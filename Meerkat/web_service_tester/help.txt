All from root directory

to start server:
	sudo ./start_server.sh

to stop the server:
	sudo ./stop_server.sh

to see if the server is online:
	sudo ./status.sh
	#if anything shows up in stdout, then it's online

